<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.ggiri.root.mybatis.project.LikeMapper">

    <insert id="insertLike" parameterType="com.ggiri.root.project.VO.LikeVO">
        INSERT INTO project_like (board_no, user_no) VALUES (#{board_no}, #{user_no})
    </insert>
    
    <delete id="deleteLike" parameterType="com.ggiri.root.project.VO.LikeVO">
        DELETE FROM project_like WHERE board_no = #{board_no} AND user_no = #{user_no}
    </delete>
    
    <select id="getLikeCount" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM project_like WHERE board_no = #{projectNum}
    </select>
    
	<!-- 좋아요 눌럿는지 안눌럿는지 -->
	<select id="findLike" resultType="int">
		select count(*) from project_like where board_no = #{board_no} and user_no = #{user_no}
	</select>
	
	<insert id="likeUp">
		insert into project_like (like_no ,board_no , user_no, freeboard_like)
		values((select * from (select max(like_no)+1 from project_like) next), #{board_no},#{user_no},1)
	</insert>
	
	<delete id="likeDown">
	  	delete from project_like where board_no = #{board_no} and user_no = #{user_no} 
	</delete>
</mapper>
